#include <bits/stdc++.h>
using namespace std;


int main()
{
	int n,i,j,a[10];
	cin>>n;

	for(i=0;i<n;i++)
	{
		cin>>a[i];
	}

	 for (int i = 0; i < n; i++) {
        int j;
        for (j=0; j<n; j++)
            if (i != j && a[i] == a[j])
                break;
        if (j == n)
            cout<<a[i];
    }

}
